# pip install redis
### to run app
#### uvicorn main:app --reload

### to build docker image
### docker compose up --build -d
### docker compose ps
### check key values for redis into redis insight tool 

### vim /opt/homebrew/etc/nginx/nginx.conf
### localhost:80/docs 
### curl -I "http://localhost/docs"